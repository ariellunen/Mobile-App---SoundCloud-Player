class Track {
  constructor(id, title, artist, image, playCount, streamUrl) {
    this.id = id;
    this.title = title;
    this.artist = artist;
    this.image = image;
    this.playCount = playCount;
    this.streamUrl = streamUrl;
  }
}

export default Track;
